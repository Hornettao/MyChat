<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fillViewport="true">
    <RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:paddingLeft="@dimen/activity_horizontal_margin"
        android:paddingRight="@dimen/activity_horizontal_margin"
        android:paddingTop="60dp"
        android:paddingBottom="@dimen/activity_vertical_margin"
        tools:context=".StartUpActivity"
        android:gravity="center_horizontal"
        android:focusable="true"
        android:focusableInTouchMode="true">

        <TextView
            android:id="@+id/text_view_password"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/pwd"/>

        <EditText
            android:id="@+id/edit_text_password"
            style="@style/LoginActivity.EditText"
            android:layout_alignLeft="@id/text_view_password"
            android:layout_below="@id/text_view_password"
            android:layout_marginTop="20dp"
            android:inputType="textPassword"
            android:ems="10"/>
        <TextView
            android:id="@+id/text_view_password_confirm"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@+id/edit_text_password"
            android:layout_marginTop="30dp"
            android:text="@string/pwd_confirm"/>
        <EditText
            android:id="@+id/edit_text_sms_code"
            style="@style/LoginActivity.EditText"
            android:layout_alignLeft="@id/text_view_password"
            android:layout_below="@id/text_view_password_confirm"
            android:layout_marginTop="20dp"
            android:inputType="textPassword"
            android:ems="10"/>

        <Button
            android:id="@+id/button_register"
            style="@style/LoginActivity.Button"
            android:layout_alignLeft="@id/text_view_password"
            android:layout_below="@id/edit_text_sms_code"
            android:layout_marginTop="30dp"
            android:text="@string/button_register_activity_register"/>
    </RelativeLayout>
</ScrollView>